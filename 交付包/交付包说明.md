# 交付包说明

- `忙忙碌碌寻宝藏-完整源码-20260724.zip`：完整源码、文档、现行 CSV 数值表、分类复核 HTML、审核证据和源素材；不包含 `.git`、`node_modules`、临时目录、构建目录或本交付包目录。
- `忙忙碌碌寻宝藏-可部署版-20260724.zip`：`dist` 目录内的生产构建，解压后根目录直接是 `index.html` 与静态资源。

当前关卡权威数据是 `数值表_src/levels_200.csv`。根目录 `数值表.xlsx` 仍为旧 100 关历史工作簿，不能覆盖 CSV。

正式发布前仍需接入任务视频成功回调、日榜/总榜后台 ID、BGM，并完成素材授权与真机验收。
