# 忙忙碌碌寻宝藏素材维护说明

## 一、当前素材结构

当前版本不再要求“6 主题 × 40 件 = 240 张”。正式内容为：

- 123 件独立物件（原有 87 件 + 新增 36 件）；
- 10 个兼容分类和物品种类、能力、材质、外观等扩展标签；
- 一件物品可以属于多个分类；
- 关卡目标是属性，例如“找出会发光的物品”，不是指定某个物品名称。

生成后的图片位于：

```text
public/items/ancient/<item_id>.webp
```

`src/game/items.ts` 是名称与属性的唯一数据源。

## 二、从两个源 zip 重新生成素材

项目根目录保留原始文件：

```text
逆水寒07小游戏素材-页面 1.zip
download (11).zip
```

执行：

```bash
npm run assets
```

脚本 `scripts/process_materials.py` 会：

1. 只保留 123 个独立画面，去掉跨分类、异尺寸和近似版本导出；
2. 根据透明通道裁掉空白；
3. 等比缩放到 224×224 安全区；
4. 居中放入 256×256 透明画布；
5. 输出质量 90 的 WebP；
6. 生成 `manifest.json` 供完整性校验。

不要直接把 zip 中约 597×553 的原图复制到 `public`。原图空白较大，会导致画面主体偏小、点击框与主体不一致，而且总体积约 16.6 MB。

## 三、修改物品名称或属性

在 `src/game/items.ts` 中修改对应条目：

```ts
{
  id: 'orange_sky_lantern',
  name: '橙色孔明灯',
  emoji: '🏮',
  img: asset('orange_sky_lantern'),
  tags: ['flying', 'glowing'],
}
```

属性 ID 定义在 `src/game/types.ts`：

```text
instrument  乐器
written     有字的物品
container   能装东西的容器
flying      会飞的
glowing     会发光的
sharp       尖锐的
sweet_food  甜的食物
plant       植物
vehicle     坐骑与交通工具
animal      动物
```

例如骏马同时属于 `vehicle` 和 `animal`；孔明灯同时属于 `flying` 和 `glowing`。不要为了跨分类再复制一张图片或创建另一个物品 ID。

## 四、新增一件物品

1. 准备透明底图片。
2. 给它确定稳定的小写英文 ID，例如 `lotus_flower`。
3. 把源图加入处理脚本，或按同样规格生成 `public/items/ancient/lotus_flower.webp`。
4. 在 `ITEMS` 数组增加定义与多维标签；桥梁等大型地标设置 `role: 'landmark'`。
5. 执行 `npm run verify:content` 和 `npm run build`。

如果修改了自动生成清单，需要同步更新 `scripts/process_materials.py` 和内容校验中的预期数量。

## 五、点击判定

- 图片无需手工标坐标。
- 每个场景物件是独立元素；普通物品随机安排，`landmark` 固定在场景下半部且不旋转。
- 玩家轻点后，代码判断该实例是否属于本轮目标属性。
- 拖动超过 8px 会被识别为探索场景，不触发误点。
- 提示道具只会定位尚未找到的正确物件。

## 六、关卡配置

正式关卡表是 `数值表_src/levels_100.csv`。核心字段：

| 字段 | 含义 |
|---|---|
| `category` | 旧关卡兼容属性与统计主分类 |
| `taskIds` | 本关的 1~3 个任务 ID，使用 `|` 分隔 |
| `targetCounts` | 各任务对应的正确物件数量，使用 `|` 分隔并与 `taskIds` 对齐 |
| `distractors` | 干扰物件数量 |

`taskIds` 的顺序就是游戏内的揭晓顺序。所有任务物件在进入关卡时一次生成，完成当前目标后只切换顶部任务提示，不刷新场景。
| `timeLimitSec` | 限时 |
| `star2` / `star3` | 二星、三星分数线 |

重新迁移/生成数值表：

```bash
npm run levels
```

该脚本会让十个分类在 100 关中各出现 10 次，生成“前 3 关单目标、4~40 关双目标、41~100 关三目标”的难度曲线，并按关卡类型重新计算目标数、时限和星级线。
