# 忙忙碌碌寻宝藏素材维护说明

## 一、当前素材结构

当前版本不再要求“6 主题 × 40 件 = 240 张”。正式内容为：

- 123 件独立物件（原有 87 件 + 新增 36 件）；
- 10 个兼容分类和物品种类、能力、材质、外观等扩展标签；
- 一件物品可以属于多个分类；
- 关卡目标是属性，例如“找出会发光的物品”，不是指定某个物品名称。

生成后的图片位于：

```text
src/assets/items/ancient/<item_id>.webp
```

`src/game/items.ts` 是名称与属性的唯一数据源。

## 二、从最新源 zip 重新生成素材

项目根目录保留当前原始文件：

```text
逆水寒07小游戏素材-0724.zip
```

执行：

```bash
npm run assets
```

脚本 `scripts/process_materials.py` 会：

1. 从 212 个带层号的物件导出中保留 123 个独立画面，去掉跨分类重复图和额外导出版本；
2. 忽略 18 张分类标题图，它们不是场景物件；
3. 根据透明通道裁掉空白；
4. 等比缩放到 224×224 安全区；
5. 居中放入 256×256 透明画布；
6. 输出质量 90 的 WebP；
7. 生成 `manifest.json` 和点击几何数据供完整性校验。

不要直接把 zip 中的 500×500 PNG 复制到生产目录。原图空白较大、同一物件会因多个分类重复导出，会导致主体偏小、点击框与主体不一致并显著增加包体。

## 三、修改物品名称或属性

在 `src/game/items.ts` 中修改对应条目：

```ts
{
  id: 'orange_sky_lantern',
  name: '橙色孔明灯',
  emoji: '🏮',
  img: asset('orange_sky_lantern'),
  tags: ['flying', 'glowing'],
}
```

属性 ID 定义在 `src/game/types.ts`：

```text
instrument  乐器
written     有字的物品
container   能装东西的容器
flying      会飞的
glowing     会发光的
sharp       尖锐的
sweet_food  甜的食物
plant       植物
vehicle     坐骑与交通工具
animal      动物
```

例如骏马同时属于 `vehicle` 和 `animal`；孔明灯同时属于 `flying` 和 `glowing`。不要为了跨分类再复制一张图片或创建另一个物品 ID。

## 四、新增一件物品

1. 准备透明底图片。
2. 给它确定稳定的小写英文 ID，例如 `lotus_flower`。
3. 把源图加入处理脚本，或按同样规格生成 `src/assets/items/ancient/lotus_flower.webp`。
4. 在 `ITEMS` 数组增加定义与多维标签；桥梁等大型地标设置 `role: 'landmark'`。
5. 执行 `npm run verify:content` 和 `npm run build`。

如果修改了自动生成清单，需要同步更新 `scripts/process_materials.py` 和内容校验中的预期数量。

## 五、点击判定

- 图片无需手工标坐标。
- 每个场景物件是独立元素；普通物品随机安排，`landmark` 固定在场景下半部且不旋转。
- 玩家轻点后，代码判断该实例是否属于本轮目标属性。
- 拖动超过 8px 会被识别为探索场景，不触发误点。
- 提示道具只会定位尚未找到的正确物件。

## 六、关卡配置

正式关卡表是 `数值表_src/levels_200.csv`。核心字段：

| 字段 | 含义 |
|---|---|
| `category` | 旧关卡兼容属性与统计主分类 |
| `taskIds` | 本关的 1~3 个任务 ID，使用 `|` 分隔 |
| `targetCounts` | 各任务对应的正确物件数量，使用 `|` 分隔并与 `taskIds` 对齐 |
| `distractors` | 干扰物件数量 |
| `timeLimitSec` | 限时 |
| `star2` / `star3` | 二星、三星分数线 |

`taskIds` 的顺序就是游戏内的揭晓顺序。所有任务物件在进入关卡时一次生成，完成当前目标后只切换顶部任务提示，不刷新场景。

重新迁移/生成数值表：

```bash
npm run levels
```

该脚本会生成 200 关、10 篇章和 9 道星门；前 3 关单目标、4~40 关双目标、41~200 关三目标，并按关卡类型重新计算目标数、时限和星级线。

分类关系需要调整时，先打开 `tools/item-classification-review.html`，在浏览器中修改并导出 JSON；把导出的 JSON 交给开发或 AI 回填 `src/game/items.ts` 与 `src/game/tasks.ts`，再执行 `npm run verify:content`。不要直接改生成后的 HTML 当作正式数据源。

## 七、主界面资源

本轮新增资源路径：

```text
src/assets/backgrounds/home-courtyard-spring-v1.webp
src/assets/ui/home/home-logo-v1.png
src/assets/ui/home/home-primary-button-v1.png
src/assets/ui/home/home-round-button-v1.png
src/assets/ui/home/home-stamina-badge-v1.png
src/assets/ui/home/home-task-tag-v1.png
```

- Logo 必须保留透明背景；按钮底图不得烘焙中文文案，文字由代码渲染，便于适配和后续改名。
- 主按钮与圆按钮要留出中央安全区，避免文字压到边框高光。
- 背景应在 390×844 竖屏中保留上部标题区、中央继续闯关按钮和底部四入口区的低细节空间。
- 替换文件时保持现有文件名和尺寸；`npm run verify:content` 会检查文件存在、格式和像素尺寸。

## 八、伙伴头像

当前映射：

| 生产文件 | 展示名 | 原始来源文件 |
|---|---|---|
| `afu.png` | 叶问舟 | `ship_npc_head_icon_yewenzhou-1x.png.png` |
| `xixi.png` | 燕无归 | `ship_npc_head_icon_yanwugui-1x.png.png` |
| `gugu.png` | 叶雪青 | `ship_npc_head_icon_yexueqing-1x.png.png` |
| `yuanyuan.png` | 方承意 | `ship_npc_head_icon_fangchengyi-1x.png.png` |
| `paopao.png` | 阿初 | `ship_npc_head_icon_achu-1x.png.png` |
| `xingxing.png` | 花将离 | `ship_npc_head_icon_huajiangli-1x.png.png` |
| `mimi.png` | 无情 | `ship_npc_head_icon_wuqing-1x.png.png` |
| `meimei.png` | 姬蜜儿 | `ship_npc_head_icon_jimier-1x.png.png` |

文件位于 `src/assets/partners/`，当前均为 128×128 透明 PNG。未获得状态不会加载姓名或头像；获得后才显示对应文件，因此无需制作未招募剪影。

当前头像适合内部功能验证；正式商业发布前必须逐张确认来源、角色形象权利和商用授权。若来源不明或与既有商业角色高度相似，应全部替换为自有统一画风，建议母版至少 1024×1024。
