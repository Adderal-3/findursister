# 《忙忙碌碌寻宝藏》本轮迭代交付说明

> 日期：2026-07-24
> 范围：用户反馈 1~15、局内体验审计、200 关扩容、日榜/总榜、伙伴展示、分类复核、文档和项目整理。

## 一、15 项反馈对应结果

| # | 需求 | 本轮结果 | 状态 |
|---:|---|---|---|
| 1 | 局内顶部失衡、关卡牌不居中 | HUD 改为“返回 / 屏幕正中关卡牌 / 暂停”三列对称布局；计时和本关分并入中央牌，左右不再互相拉偏 | 已完成 |
| 2 | 目标展示、完成提示、下一题提示 | 顶部改为任务轨道，同时显示已完成、当前、下一项；阶段完成显示“X 完成 / 下一项 Y” | 已完成 |
| 3 | 首次赠送加时，第二次进任务视频流 | 每关第一次免费 +15 秒；第二次暂停计时并进入大神任务面板。奖励发放回调 `grantRewardedTimeBoost()` 已预留 | 前端完成，待任务回调 |
| 4 | 第一次正确点击无音效 | 在局内 `pointerup` 同步执行音频解锁，再进入命中判定，保证移动端首击也有正确音效 | 已完成 |
| 5 | 静音按钮和未来 BGM | 局内左下新增声音/静音按钮并持久化；`VITE_GAME_BGM_URL` 配置后自动循环并受静音控制 | 已完成，待 BGM 文件 |
| 6 | 100 关太少 | 主线扩为 200 关、10 篇章、每篇 20 关、9 道星门；校验脚本同步升级 | 已完成 |
| 7 | 分类对照表和后续修改流程 | 新增可编辑 `tools/item-classification-review.html`，支持搜索、五域改标、备注、任务影响、草稿、导入与导出 JSON/CSV | 已完成 |
| 8 | 专业视角整体体验迭代 | 完成局内、伙伴、榜单、成长线与沉浸感审计；剩余高优先级见本文第三节 | 已完成 |
| 9 | 伙伴弹窗部分清晰部分模糊、按钮边框糊 | 移除背景 `backdrop-blur`；弹窗改不透明底；底部按钮改为 CSS 实线边框和渐变，不再拉伸软边位图 | 已完成 |
| 10 | 主界面标题太小 | Logo 宽度由 90% 提到 108%，并上移到更明确的主视觉区域 | 已完成 |
| 11 | 伙伴获取方式后续可改；删指定文案 | 删除“八条成长路线并行推进”；伙伴规则集中在 `src/game/partners.ts`，修改方法写入 README；未获得卡仅显示获得方式 | 已完成 |
| 12 | 局内素材太小、太空旷 | 最终收口为移动端单屏画布；物件基准尺寸从短边 19% 提至 20.5%，完整可见轮廓装箱，主体不裁切、不互压 | 已完成 |
| 13 | 优化单局/排行榜；榜单只留日榜和总榜 | 每关原始分保留；总榜改为每关等权标准分，避免后期关目标多造成权重畸变；榜单页只显示日榜/总榜，不再展示公式 | 已完成，远程榜待 ID |
| 14 | 清理项目、更新文档、说明缺口 | 更新 README、体验审计、玩法设计/上线说明、后台配置与本交付说明；生成干净交付包，临时目录和旧部署包清理 | 已完成 |
| 15 | 未获得伙伴不展示名称 | 未获得状态不渲染伙伴姓名和头像，只显示“获得方式”、说明、当前进度和锁图标 | 已完成 |

## 二、计分与榜单的新口径

### 1. 单局

- 单次命中仍为 `剩余时间 × 连击倍率`，玩家已有手感和星级阈值不被破坏。
- 星级、伙伴“单关高分”条件、关卡历史最好继续使用原始分。
- 免费/任务加时会抬高后续命中分，但标准榜分有 1200 的单关上限，避免加时无限放大。

### 2. 总榜

旧逻辑直接把所有关卡原始分相加。后期关目标更多、时限更长，因此单关原始分可能是前期关的数倍，会让排行榜被少数后期关主导。

新逻辑：

```text
单关标准榜分 = round(min(1.2, 历史最好原始分 / 本关理论满分) × 1000)
总榜基础分 = 所有单关标准榜分之和
总榜最终分 = round(总榜基础分 × (1 + 伙伴人数 × 1.25%))
```

这样每关正常权重相同，玩家是在比较“这一关完成得有多好”，不是比较“这一关有多少物件”。

### 3. 日榜

- 当日每个不同关卡只保留一个最好成绩。
- 转成标准榜分后求和。
- 不吃伙伴加成，减少长期养成对每日竞争的压制。
- 本地按设备日期重置；正式版应由大神日榜周期或可信服务按统一时区重置。

### 4. 榜单页面

- 只保留“日榜 / 总榜”两个页签。
- 计分解释移到 README 和游戏说明，不占榜单内容区域。
- 未配置大神 ID 时展示本机成绩；配置后切换远程玩家排名。

## 三、专业体验审计

### 总体判断

当前版本从“能玩的素材验证版”进入了“核心闭环可测试版”。最明显的三处断裂已经补上：

1. 任务没有上下文 → 现在有当前/已完成/下一项目标；
2. 场景小而散 → 现在首屏更密、物件更大；
3. 成长系统解释过多但竞争入口混杂 → 榜单只保留日榜/总榜，规则移到说明。

### 流程健康度

| 步骤 | 玩家看到与执行的事情 | 健康度 |
|---:|---|---|
| 1 | 首页确认标题、眼力值和唯一主按钮 | 良好；标题层级已经加强 |
| 2 | 进入关卡并理解当前任务 | 良好；顶部真正居中，信息集中 |
| 3 | 拖动、搜索并点击物件 | 良好；物件更大更密，命中仍使用透明像素蒙版 |
| 4 | 完成一个子目标并切换下一项 | 良好；有显式完成和下一项提示 |
| 5 | 时间不足时使用加时 | 部分完成；免费链路可用，视频链路等任务系统回调 |
| 6 | 过关、查看星级和榜分增量 | 良好；原始分与标准榜分分层 |
| 7 | 查看/获得伙伴 | 良好；未获得信息不泄露姓名，弹窗清晰度已修 |
| 8 | 查看日榜和总榜 | 部分完成；界面与本地逻辑可用，真实排行依赖后台 ID |

### 下一轮最值得做的 5 件事

1. **接真实任务视频奖励闭环**：完播、验签、幂等和回调成功态必须先于任何付费/奖励投放。
2. **做每日委托和每日首胜**：200 关解决内容量，但“今天为什么回来”仍需要明确答案。
3. **为第 101~200 关增加规则事件**：目前任务组合和特殊关会轮换，长期仍可能感到重复；建议新增移动物件、局部照明、限错次数等规则。
4. **补失败诊断**：根据误点多、拖动少、剩余目标和时间消耗，结算只给一条可执行建议。
5. **真机可访问性和性能验收**：360×640、刘海屏、Safari、主流安卓、低端机；同时检查色彩对比、44px 点击目标、屏幕阅读器标签和减少动态效果。

## 四、分类调整的推荐工作流

1. 打开 `tools/item-classification-review.html`。
2. 先用“只看未调整”逐类检查基础分类；基础分类会直接影响常规关目标池，优先级最高。
3. 再检查物品种类、能力状态、材质和外观；这些会影响组合题。
4. 每次只修改你确定的标签，备注写“为什么改”，不要先改任务。
5. 导出 `物品分类调整.json` 发给 AI。
6. AI 更新 `src/game/items.ts` 后运行：

```text
npm run catalog
npm run catalog:review
npm run levels
npm run check
```

7. 如果某个任务候选少于目标数，校验会明确指出任务 ID，再决定是补标签、降目标数还是删除该题。

## 五、你还需要补充的内容

| 内容 | 你需要提供 | 接入位置 |
|---|---|---|
| 任务视频流 | 大神活动 ID、任务 ID、完成/领奖回调字段、是否允许每关多次 | `.env`、`src/platform/ds/runtime.ts`、`grantRewardedTimeBoost()` |
| 日榜/总榜 | 测试/正式小游戏 ID；测试/正式日榜 ID；测试/正式总榜 ID；日榜重置时区 | `.env.example` 对应 6 个字段 |
| BGM | 已确认版权的 mp3/ogg、目标音量、是否首页即播 | `public/audio/`、`VITE_GAME_BGM_URL` |
| 伙伴获取方式 | 新的 8 位伙伴条件、阈值、文案和是否影响总榜 | `src/game/partners.ts` |
| 分类修改 | 从对照表导出的 JSON | `src/game/items.ts` |
| 正式素材 | 物件、伙伴、背景、Logo、分享图的商业授权证明 | 发布清单 |

## 六、证据与验证边界

- 用户提供并已检查的改造前证据：
  - `审核证据/01-局内改造前.png`
  - `审核证据/02-伙伴弹窗改造前.png`
  - `审核证据/03-移动端主页裁切问题.png`
  - `审核证据/04-移动端局内边缘裁切问题.png`
- 代码验证：`npm run check` 已覆盖 123 件物品、30 条任务、200 关、8 位伙伴、ESLint、TypeScript 和 Vite 正式构建。
- 当前环境未提供可控制的应用内浏览器，因此本轮无法生成可信的“改造后同机型截图”；真机视觉、触摸、音频自动播放和任务弹层仍需在目标设备验收。
- `数值表.xlsx` 的专用表格运行库在当前环境中不可用，因此没有安全重写旧工作簿；当前权威关卡数据为 `数值表_src/levels_200.csv`。
