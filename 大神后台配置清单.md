# 《找你妹大作战》大神后台配置清单

> 用途：项目代码已经接好大神 SDK，不写入任何假 ID。完成下面配置后，把拿到的值填入项目根目录 `.env.local`，重新执行 `npm run check` 即可。

## 当前接入状态

| 能力 | 当前状态 | 已有/仍需配置 |
|---|---|---|
| App 与圈子 | 已接入 | `appKey=L36`；正式圈子 ID `60a61f832a3be16207f9651c` |
| 分享 | 已接入 | 标题、描述、分享图均已写入默认配置与页面分享元信息 |
| 任务 | 前端入口与 `TaskModule` 已接入 | 仍需正式 `VITE_DS_ACT_ID` |
| 绑角 | 首页角色入口与 `Role.evoke` 已接入 | 与任务共用正式 `VITE_DS_ACT_ID` |
| 排行榜 | 页面、读榜与结算写入已接入 | 仍需测试/正式小游戏 ID 与测试/正式榜单 ID |
| DataHub | 字段清单已生成 | 在 CMS 导入/登记 `ds-datahub-keys.json` |

本地 `localhost` 预览会主动关闭大神登录与远端请求，因此可以直接调试游戏；正式 HTTPS 域名构建会按上述 ID 自动启用对应能力。

## 一、需要办理的后台标识

| 顺序 | 去哪里做 | 需要拿到 | 项目配置项 | 用途 |
|---|---|---|---|---|
| 1 | 大神游戏/应用配置 | 游戏 `appKey` | `VITE_DS_APP_KEY` | 登录、活动 SDK、埋点中的游戏标识 |
| 2 | 大神圈子/广场配置 | `squareId` | `VITE_DS_SQUARE_ID` | Ulink 拉起大神并进入对应圈子、分享归属 |
| 3 | 大神活动后台（外网环境） | 活动 `actId` | `VITE_DS_ACT_ID` | 拉取任务列表、登录态、任务完成状态与领奖 |
| 4 | 小游戏数据后台（测试） | 测试小游戏 ID | `VITE_DS_DEV_MINI_GAME_ID` | 测试环境数据读写 |
| 5 | 小游戏数据后台（正式） | 正式小游戏 ID | `VITE_DS_PRO_MINI_GAME_ID` | 正式环境数据读写 |
| 6 | 排行榜后台（测试） | 测试榜单 ID | `VITE_DS_DEV_BILLBOARD_ID` | 测试环境排行榜 |
| 7 | 排行榜后台（正式） | 正式榜单 ID | `VITE_DS_PRO_BILLBOARD_ID` | 正式环境排行榜 |

其中：

- `appKey + squareId` 填好后，登录、Ulink、大神内分享菜单、导航栏和 NS 业务埋点生效。
- `appKey + actId` 填好后，首页任务按钮和角色绑定会连接真实后台；未填写时保留可识别的本地占位状态，但不会发出空请求。
- 四个小游戏/榜单 ID 全部填好后，排行榜切换为大神总榜；未填写时显示本机记录。
- 没填后台 ID 时项目保持普通静态 H5，不白屏、不弹登录。

## 二、大神活动后台要做的事

1. 新建一个正式活动，活动名建议用“找你妹大作战”。
2. 绑定本游戏的 `appKey`，确认活动的开始时间、结束时间和服务器时区。
3. 配置任务列表、任务文案、跳转地址、奖励内容、库存和每账号领取次数。
4. 确认每个任务是否允许站外 H5 参与，即后台字段 `nonAppCanJoin` 的口径。
5. 如果任务要求绑定游戏角色，在后台开启角色绑定并确认最低角色等级；同时将 `.env.local` 的 `VITE_DS_TASK_SHOW_ROLE` 设为 `true`。
6. 用测试账号分别验证：未登录、已登录未完成、已完成可领取、已领取、活动未开始、活动已结束、奖励库存不足。
7. 将最终活动切到正式环境，再把正式 `actId` 写入 `.env.local`，不要把测试 ID 混进正式包。

当前前端已接入 `DsActSdk.TaskModule`。它可以自动拉取后台任务列表，处理登录态、自动签到、平台分享补打、状态展示和领奖预检。它不能凭空判断“玩家完成第 N 关”等游戏内自定义行为；若后台要配这类任务，必须再由大神后台/接口负责人提供对应的任务上报 API 或服务端判定方案。当前 Skill 没有提供通用的自定义完成上报接口，因此不要只靠前端埋点发放有价值奖励。

## 三、域名与发布配置

1. 准备正式 HTTPS 访问地址，禁止把 `localhost` 或局域网地址填到后台。
2. 在大神相关后台把正式域名加入 H5 活动、登录回调、分享/跳转白名单；具体入口名称以你拿到的后台版本为准。
3. 确认部署平台会在页面运行时注入 `window.DA_SQUARE_ID`、`window.DA_GROUP_ID`、`window.DA_PROJECT_ID`。这三个值由部署环境注入，项目代码里不要定义。
4. 上传最终分享图到可公网访问的 HTTPS CDN，填写 `VITE_DS_SHARE_ICON`。建议 1:1、至少 512×512、PNG/JPG、无透明细线小字。
5. 如果发布平台要求 ZIP，上传项目生成的 `deploy.zip`；ZIP 根目录必须直接包含 `index.html` 和 `assets/`，不能再套一层 `dist/`。

## 四、在项目里填写配置

复制 `.env.example` 为 `.env.local`，只填写真实后台值：

```dotenv
VITE_DS_APP_KEY=L36
VITE_DS_SQUARE_ID=60a61f832a3be16207f9651c
VITE_DS_ACT_ID=真实_actId
VITE_DS_H5_LOGIN_ENABLED=true
VITE_DS_SHARE_TITLE=忙忙碌碌寻宝藏
VITE_DS_SHARE_DESC=挑战眼力值，寻找汴京珍宝
VITE_DS_SHARE_ICON=https://img.166.net/gameyw-misc/opd/squash/20260630/101947-goc2wybfsi.png
VITE_DS_DEV_MINI_GAME_ID=测试小游戏ID
VITE_DS_PRO_MINI_GAME_ID=正式小游戏ID
VITE_DS_DEV_BILLBOARD_ID=测试榜单ID
VITE_DS_PRO_BILLBOARD_ID=正式榜单ID
```

其他值可先保留默认。`VITE_DS_FRONT_ID` 仅 CPS 底部栏需要，本项目当前没启用 CPS，留空即可。

配置后执行：

```bash
npm run check
npm run build
```

然后重新压缩 `dist` 目录内容。Vite 会在构建时写入环境变量，所以修改 `.env.local` 后必须重新构建，不能只替换 ZIP 里的 HTML。

## 五、本轮暂不配置

- 激励视频签发、完播验签和奖励发放：按你的安排，后续与任务面板一起做。
- 分享行为奖励：按你的安排暂缓；普通分享菜单能力已接好，但当前不根据“调起分享”发奖。
- CPS 底部栏：没有业务需求，暂不启用，因此不需要 `frontId`。
- 回流任务 `TaskChecker`：需要 `asId` 和明确回流规则，当前不启用。

## 六、后台验收结果表

上线前请逐项填“通过”：

| 验收项 | 结果 |
|---|---|
| 正式 appKey / squareId / actId 已填入正式构建环境 | 待办 |
| 测试/正式小游戏 ID 与测试/正式榜单 ID 已填入构建环境 | 待办 |
| `ds-datahub-keys.json` 中 4 个字段已在 CMS 登记 | 待办 |
| 正式域名 HTTPS 可访问且已加入白名单 | 待办 |
| 普通浏览器 H5 登录成功 | 待办 |
| 大神 App 内登录、关闭、分享菜单和导航栏正常 | 待办 |
| Ulink 能拉起大神并进入正确圈子 | 待办 |
| 任务面板能显示真实后台任务 | 待办 |
| 角色选择、绑定与已绑角色回显正常 | 待办 |
| 大神总榜、本人名次与结算后分数同步正常 | 待办 |
| 可领取、已领取、库存不足和活动结束状态正确 | 待办 |
| 分享标题、描述、图标为最终素材 | 待办 |
| 部署环境正确注入 DA 三个统计维度 | 待办 |
