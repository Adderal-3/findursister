# 《忙忙碌碌寻宝藏》大神后台配置清单 v3.5

> 代码不写入假 ID。拿到真实配置后写入项目根目录 `.env.local`，重新执行 `npm run check`。

## 一、需要办理的后台标识

| 顺序 | 标识 | 环境变量 | 用途 |
|---|---|---|---|
| 1 | 游戏 appKey | `VITE_DS_APP_KEY` | 登录、活动 SDK、埋点 |
| 2 | 圈子 squareId | `VITE_DS_SQUARE_ID` | Ulink 与分享归属 |
| 3 | 活动 actId | `VITE_DS_ACT_ID` | 任务列表、状态与领奖 |
| 4 | 测试小游戏 ID | `VITE_DS_DEV_MINI_GAME_ID` | 测试数据读写 |
| 5 | 正式小游戏 ID | `VITE_DS_PRO_MINI_GAME_ID` | 正式数据读写 |
| 6 | 测试总榜 ID | `VITE_DS_DEV_BILLBOARD_ID` | 测试环境总榜 |
| 7 | 正式总榜 ID | `VITE_DS_PRO_BILLBOARD_ID` | 正式环境总榜 |
| 8 | 测试日榜 ID | `VITE_DS_DEV_DAILY_BILLBOARD_ID` | 测试环境日榜 |
| 9 | 正式日榜 ID | `VITE_DS_PRO_DAILY_BILLBOARD_ID` | 正式环境日榜 |

## 二、DataHub 字段

| 字段 | 含义 | 后台用途 |
|---|---|---|
| `levels_daily_score` | 当日每关最好标准榜分之和，不含伙伴加成 | 日榜排序 |
| `levels_total_score` | `round(标准榜分合计 × 伙伴系数)` | 总榜排序 |
| `levels_base_score` | 未套伙伴系数的历史标准榜分合计 | 客服与风控审计 |
| `partner_bonus_bps` | 每位伙伴 125 基点，满 8 位 1000 | 加成审计 |
| `level_max` | 最高解锁关卡 | 进度任务/展示 |
| `endless_best` | 无尽历史最高分 | 后续无尽记录 |
| `last_save` | 最后同步时间戳 | 同步诊断 |

单关标准榜分公式：

```text
round(min(1.2, 单关历史最高原始分 / starBase) × 1000)
```

日榜和总榜逻辑当前是暂定版，正式后台还需确认：

- 日榜使用哪个服务器自然日与时区。
- 同分时按先达到、关卡数还是其他字段排序。
- 日榜是否由榜单周期自动清空，或需要日期分桶。
- 伙伴新增后总榜实时重算的刷新频率。
- 成绩签名、异常速度、设备切换和重复提交的风控策略。

## 三、任务视频加时

前端已经完成“每关首次免费 +15 秒；第二次打开任务视频流程”的入口。后台/任务系统需要提供：

1. 打开指定任务或视频流的调用方式。
2. 完播、取消、加载失败和中断回调。
3. 可用于幂等的 `rewardId` 或等价标识。
4. 每关、每账号、每日的奖励次数口径。
5. 验签位置和可信发奖方。

观看期间前端会暂停倒计时和局内输入；只有可信成功回调后才调用加时发放函数。

## 四、环境变量模板

```dotenv
VITE_DS_APP_KEY=l36
VITE_DS_SQUARE_ID=60a61f832a3be16207f9651c
VITE_DS_ACT_ID=真实_actId
VITE_DS_DEV_MINI_GAME_ID=测试小游戏ID
VITE_DS_PRO_MINI_GAME_ID=正式小游戏ID
VITE_DS_DEV_BILLBOARD_ID=测试总榜ID
VITE_DS_PRO_BILLBOARD_ID=正式总榜ID
VITE_DS_DEV_DAILY_BILLBOARD_ID=测试日榜ID
VITE_DS_PRO_DAILY_BILLBOARD_ID=正式日榜ID
VITE_GAME_BGM_URL=/audio/bgm.mp3
```

修改 `.env.local` 后必须重新构建；Vite 环境变量在构建时写入。

## 五、上线验收

| 验收项 | 状态 |
|---|---|
| 正式 appKey / squareId / actId 已填入正式环境 | 待办 |
| 测试/正式小游戏、日榜、总榜 ID 已填入 | 待办 |
| `ds-datahub-keys.json` 的 7 个字段已登记 | 待办 |
| 日榜绑定 `levels_daily_score` | 待办 |
| 总榜绑定 `levels_total_score` | 待办 |
| 服务器自然日与同分规则已书面确认 | 待办 |
| 任务视频成功/取消/失败与幂等验证通过 | 待办 |
| 大神 App 内登录、任务、分享、导航栏正常 | 待办 |
| 结算后日榜/总榜分数与个人名次同步正常 | 待办 |
| 正式域名 HTTPS 与白名单完成 | 待办 |

## 六、当前不应仅靠前端实现

- 激励视频验签和有价值奖励发放。
- 公平的跨设备排行榜与防作弊。
- 自定义任务的可信完成判定。
- 赛季结算、库存、实物或高价值奖励。
