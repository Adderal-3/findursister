# Ulink 块

- [ ] `isGodlike` 为真时提前 return（App 内不走 ulink）
- [ ] `onDsUlinkReady` 回调模式存在（禁止直接 `new DsUlink` 不等 ready）
- [ ] `initTraceInstall()` 已调用
- [ ] `openSquareUrl` 的 params 含 `url: window.location.href` 回调地址
