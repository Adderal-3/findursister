# 《忙忙碌碌寻宝藏》最终上线任务文档 v3.5

> 更新：2026-07-24
> 当前权威内容：`src/game/items.ts`、`src/game/tasks.ts`、`数值表_src/levels_200.csv`、`src/game/partners.ts`。
> 根目录 `数值表.xlsx` 仍是旧 100 关工作簿，只能历史参考，不能覆盖当前 CSV。

## 一、当前可交付范围

| 模块 | 当前状态 | 说明 |
|---|---|---|
| 200 关主线 | 已完成 | 10 篇章、20 小章、9 道星门、200 关地图与旧关重玩 |
| 局内核心体验 | 已完成 | 对称 HUD、当前/后续目标轨道、完成提示、下一项目标提示 |
| 场景密度 | 已完成 | 移动端单屏画布、物件基础尺寸提高、完整轮廓装箱；初始画面不再切出半个物件 |
| 加时 | 部分完成 | 每关首次免费 +15 秒；第二次打开任务视频面板，等待真实任务回调 |
| 音频 | 已完成接口 | 首次正确点击可解锁并播放音效；静音持久化；支持可选循环 BGM |
| 伙伴 | 已完成前端 | 8 条获得路径；未获得不显示姓名/头像，仅显示获得方式和进度 |
| 排行榜 | 部分完成 | 玩家端只保留日榜/总榜；本地等权标准分可用，正式远端榜待 ID |
| 分类复核 | 已完成工具 | `tools/item-classification-review.html` 支持编辑、导入导出 JSON/CSV |
| 大神平台 | 部分完成 | SDK 安全降级已接；任务、日榜、总榜需真实后台参数和可信回调 |

## 二、局内现行规则

- 顶部采用左右对称按钮和真正居中的关卡牌。
- 首页 Logo 的居中定位与 Motion 动画已分层，移动端不再从屏幕中线开始导致右侧裁切。
- 任务区一次展示全部目标：完成项、当前项、后续项状态不同。
- 新手规则首次进入时一次讲完，L2/L3 不再继续弹出教学。
- 每完成一项会明确提示“已完成”和“下一项是什么”；全部完成进入结算。
- 每关首次点击加时直接赠送 +15 秒；第二次点击暂停游戏并进入任务视频流程。
- 提示按钮仍保留每局首次免费次数。
- 静音按钮在局内常驻，状态写入本地存档。
- BGM 文件可放到 `public/audio/bgm.mp3`，并在 `.env.local` 配置：

```dotenv
VITE_GAME_BGM_URL=/audio/bgm.mp3
```

## 三、200 关结构

- 10 个篇章，每篇 20 关。
- 20 个小章，每 10 关一个 Boss。
- 9 道星门：第 21/41/61/81/101/121/141/161/181 关。
- 星门要求：15/45/80/120/165/215/270/330/395 星。
- L1~3 单目标，L4~40 双目标，L41~200 三目标。
- 正式关卡表：`数值表_src/levels_200.csv`。

## 四、分数与榜单

原始分继续服务局内爽感、星级和伙伴条件。排行榜另做单关等权折算：

```text
单关标准榜分 = round(min(1.2, 单关历史最高原始分 / starBase) × 1000)
总榜基础分 = 所有单关标准榜分之和
总榜最终分 = round(总榜基础分 × (1 + 已获得伙伴数 × 1.25%))
```

- 单关达到 `starBase` 时为 1000 榜分，封顶 1200。
- 日榜：当天每个关卡只保留最高原始分，再折算后求和；不吃伙伴加成。
- 总榜：历史每关最高标准榜分求和；伙伴加成实时生效。
- 玩家端榜单不展示公式，只展示日榜/总榜结果；规则放在 README 和玩法说明。
- 正式日榜自然日、同分规则、防作弊和结算以服务端为准。

## 五、分类调整流程

1. 打开 `tools/item-classification-review.html`。
2. 搜索物件，点击卡片，修改主分类、标签和备注。
3. 使用“导出 JSON”保存修改稿。
4. 把 JSON 交给开发或 AI，回填 `src/game/items.ts` 与 `src/game/tasks.ts`。
5. 执行 `npm run verify:content` 与 `npm run build`。

HTML 是复核工作台，不是生产数据源；最终权威仍在 TypeScript 数据文件。

## 六、伙伴修改方式

- 伙伴名称、头像、条件、阈值和文案统一维护在 `src/game/partners.ts`。
- 伙伴页面布局与锁定态在 `src/components/HomePanels.tsx`。
- 招募成功弹窗在 `src/components/PartnerRecruitDialog.tsx`。
- 修改后执行 `npm run check`，并至少验证未获得、单人获得、多人连续获得、全员获得四种状态。
- 当前锁定态只显示获得方式和进度；不要提前暴露姓名或头像。

## 七、上线前仍需补齐

| 依赖 | 需要提供 |
|---|---|
| 任务视频加时 | 任务面板入口、完播回调、成功/取消/失败状态、奖励幂等 ID |
| 远程日榜 | 测试/正式日榜 ID、服务器自然日时区、同分规则 |
| 远程总榜 | 测试/正式总榜 ID、可信成绩写入、防作弊与刷新频率 |
| BGM | 最终音频文件、循环点和商业授权记录 |
| 素材授权 | 123 件物品、8 位伙伴、Logo、背景、UI 的可追溯商用授权 |
| 真机验证 | 360×640、小刘海/大刘海、主流安卓、iPhone Safari、大神 App 内 WebView |
| 分类决策 | 从复核 HTML 导出的最终 JSON |

## 八、上线验收

| 类别 | 验收标准 |
|---|---|
| 自动检查 | `npm run check` 通过 |
| 首屏 | 360×640 可操作，无横向溢出或白屏 |
| 局内 | 开始、拖动、点击、误点、提示、加时、静音、暂停、过关、失败、重试正常 |
| 任务轨道 | 每个目标的完成/当前/后续状态与下一项提示准确 |
| 存档 | 关卡、星级、历史最高分、伙伴、静音、每日分数刷新后保留 |
| 排行榜 | 日榜/总榜分别读写正确；榜单内无计算公式 |
| 安全 | 无真实密钥进入前端；高价值奖励不由本地布尔值直接发放 |
| 素材 | 所有正式素材有商用授权凭证 |
| 打包 | 源码交付包不含 `node_modules`、临时目录、日志、`.env.local` 或私钥 |

## 九、本轮结论

代码层已经完成用户反馈中的可本地完成部分，并把外部依赖预留成明确接口。当前可作为完整本地原型和后台联调基线；正式上线仍取决于任务回调、榜单 ID、统一自然日、可信服务、素材授权和真机验证。
