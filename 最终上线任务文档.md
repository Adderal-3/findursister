# 《百物寻踪》最终上线任务文档

> 版本：v2.2（2026-07-23，多目标顺序揭晓版）  
> 目标：发布为一个不自建业务后端的移动端静态 H5；可使用大神平台已有登录、跳转、分享、导航、统计和活动任务服务。  
> 当前权威内容：`src/game/items.ts`、`src/game/tasks.ts`、`数值表_src/levels_100.csv`。`数值表.xlsx` 与 `玩法设计文档.md` 中的 240 件/六主题内容属于旧版长期规划，尚待后续重算。

## 一、上线范围与结论

本项目现在已经可以作为“纯静态单机寻物 H5”构建和部署，现有进度保存在本机浏览器。大神基础 SDK 与任务面板入口也已接好；后台 ID 未填写时全部安全关闭，不影响普通 H5 游玩。

首发建议采用两档范围：

| 范围 | 首发建议 | 原因 |
|---|---|---|
| 第 1~100 关多目标闯关 + 当前无尽模式 | 可以公开测试 | L1~3 单目标、L4~40 双目标、L41~100 三目标；同关目标逐个揭晓且共用同一紧凑场景 |
| 特殊关表现 | 可以公开测试 | 集簇目标聚集、迷踪雾层、夜航暗场、极速短时限、Boss 高目标与压迫边框均已落地；仍需真机调参 |
| 大神任务面板 | 后台配置后可以开放 | SDK 已接入；平台可管理列表、状态和领奖，但游戏内自定义任务还需确认上报接口 |
| 分享菜单 | 已预埋，不做奖励 | 按本轮范围不把分享调起当完成或发奖依据 |
| 激励视频 | 暂缓 | 需要签发、完播验签、`rewardId` 幂等与奖励口径，后续接任务面板时处理 |

### 不自建后端时能做到

- 闯关与当前无尽模式、随机场景、目标点击、提示、音效、暂停和结算。
- 100 关 CSV 数值读取、每关历史最高分、星级、最远关卡、本地续玩。
- 大神 App/H5 登录壳、圈子 Ulink、大神导航栏、普通分享配置、NS 页面和业务埋点。
- 大神活动 SDK 提供的任务列表、自动签到、平台任务状态、领取面板和活动预检。
- 静态 CDN/对象存储部署，无需数据库、Node/PHP 服务或自建 API。

### 不自建后端不能可靠做到

- 跨设备同步存档、账号找回、全服排行榜、赛季结算和可信发奖。
- 防作弊的正式积分、体力、伙伴、图鉴加成和每日次数判定。
- 自定义“通关 N 次/达到 N 分”任务的可信完成判定，除非大神另有服务端任务上报接口。
- UGC 关卡码签名、公开关卡池、举报审核、有效完成去重和分享奖励。
- 有价值的激励视频/分享奖励验签。纯前端可被修改，不能作为资产发放依据。

## 二、游戏玩法

### 2.1 核心循环

1. 玩家选择闯关或无尽模式。
2. 进入比屏幕更大的虚拟场景，按住拖动探索。
3. 顶部每次只展示当前属性目标；完成后在同一场景中揭晓下一个，不提前暴露本关目标总数。
4. 找对获得“剩余时间 × 连击倍率”的分数；找错扣 3 秒并清空连击。
5. 找齐目标后闯关结算；超时则失败。无尽模式找齐后自动进入下一波。
6. 闯关保存每关历史最好分与星级，玩家可继续下一关或回炉刷星。

### 2.2 操作与判定

| 操作 | 结果 |
|---|---|
| 按住并拖动 | 水平探索 2.2×1 屏的大场景 |
| 轻点目标 | 目标数量 -1、累计分数、连击 +1 |
| 轻点干扰物或空白 | 剩余时间 -3 秒、连击归零 |
| 点击提示 | 消耗 1 次提示，短暂高亮一个未找到目标 |
| 暂停 | 停止计时，允许继续或退出 |

每个物品是独立 DOM 元素，点击区域跟随图片自身，不需要给每张替换素材重新标坐标。场景坐标、缩放和旋转由生成器随机产生。

### 2.3 计分、连击与星级

- 单次得分：`剩余时间 × 连击倍率`，代码目前保留 0.1 分精度。
- 连击倍率：第 1 击 1.0，第 2 击 1.1，逐击 +0.1；第 10 击起封顶 1.9。
- 连击无时间窗口，只有点错才归零。
- 过关至少 1 星；达到关卡表 `star2` / `star3` 得 2 / 3 星。
- 闯关总分为各关历史最高基础分之和，重复刷低分不会降低记录。

### 2.4 当前无尽模式

- 75 秒起步，每找对 1 件 +2 秒，最高回到 99 秒。
- 找齐一波自动切换主题并提高目标/干扰项密度。
- 点错仍扣 3 秒并断连击；无提示次数。
- 当前为早期版。长期设计“百宝深渊”应在 60 关后解锁、消耗 5 眼力值，并改用 `endless_curve.csv`；这部分尚未实现。

## 三、关卡玩法

### 3.1 内容结构

- 总量：100 关。
- 大结构：5 个篇章，每篇 20 关。
- 数值结构：10 个小章，每章 10 关；第 10/20/.../100 关为 Boss。
- 正式素材：123 件独立古风物件，总体积约 1.87 MiB。
- 属性分类：乐器、有字、容器、会飞、会发光、尖锐、甜食、植物、交通工具、动物，共 10 类；一件物品可拥有多个属性。

| 篇章 | 关卡 | 内容目标 |
|---|---:|---|
| 初识百物 | 1~20 | L1~3 单目标教学，L4 起双目标，学习拖动、属性判断与剩余时间计分 |
| 属性进阶 | 21~40 | 双目标加入复合条件，并出现首批特殊关 |
| 迷踪夜巡 | 41~60 | 升级为三目标，提高密度并加入迷踪/夜巡节奏 |
| 百物狂潮 | 61~80 | 三目标高密度轮换，单目标时间预算继续收紧 |
| 火眼金睛之路 | 81~100 | 11~12 个总目标，全要素混合且特殊关高频出现 |

### 3.2 关卡类型及开发状态

| 类型 | 规则 | 首次出现 | 当前状态 |
|---|---|---:|---|
| 标准关 | 随篇章从单目标升级到双/三目标 | 1 | 已实现 |
| Boss 关 | 每 10 关一次；目标从 6 件提升至 12 件，时间系数收紧 | 10 | 核心规则与表现已实现；首通奖励尚未实现 |
| 集簇关 | 多组目标更集中地出现在局部区域 | 24 | 已实现 |
| 迷踪关 | 高密度干扰并叠加雾层 | 27 | 基础规则已实现；视觉近似物优先仍待补 |
| 极速关 | 更短时限，强调反应速度 | 35 | 已实现 |
| 夜航关 | 暗色氛围下完成多目标寻物 | 44 | 基础暗场规则已实现；动态局部照明仍待补 |

100 关已具备可感知的目标数、时间预算、物件密度与特殊关表现梯度；正式上线前仍需用真实玩家数据校准通过率和三星率。

### 3.3 解锁与奖励待办

- 第 21 / 41 / 61 / 81 关星门：15 / 45 / 80 / 120 星。
- 增加关卡选择页，让玩家可以重玩任意已解锁关卡。
- 每小章 Boss 首通：沙漏 +1；小章通关：放大镜 +3。
- 第 100 关首通：称号“寻物宗师”与无尽高阶入场特效。
- 上述奖励系统依赖体力/道具/称号模块，当前尚未入代码。

## 四、已完成的大神平台接入

| 能力 | 实现 | 未配置时行为 |
|---|---|---|
| NS 页面统计与全局错误上报 | Skill 指定 loader 与统计维度 | 页面仍正常运行 |
| 业务埋点 | 开始、命中、点错、过关、失败、提示、打开任务面板 | 不发送业务事件 |
| 大神 JSSDK | 1.0.87，登录、登录预检、App 分享菜单 | 普通静态模式 |
| Universal Login | 2.1.4，支持站外 H5 登录 | 不弹登录 |
| Ulink | 站外拉起大神并进入指定圈子 | 不跳转 |
| Mobile Share | 2.2.0 普通 H5 分享配置 | 不影响游戏 |
| 大神导航栏 | 0.0.5，登录后初始化、全屏、关闭/菜单/状态栏回调 | 站外不渲染 |
| DS Act 任务面板 | 0.1.5，真实 appKey+actId 存在时显示入口 | 入口隐藏 |
| 静态存档 | 保留既有 key；localStorage 不可用时退化为本次会话内存存档 | 可玩但刷新后丢失 |

后台操作详见 `大神后台配置清单.md`。配置模板为 `.env.example`，真实值写入 `.env.local`。

## 五、资源替换清单与来源

### 5.1 已完成：123 件古风寻物物件

当前正式版本已接入两批素材。生产版本按画面与语义去重为 123 件；跨分类关系改由 `tags` 表达，不再复制图片。

规格建议：

- WebP 透明底，256×256 正方形画布。
- 主体位于 224×224 安全区，自动裁除导出空白。
- 单张全部 ≤100 KB，123 张总计约 1.87 MiB。
- 文件名使用稳定的小写英文 ID；标签与中文名统一维护在 `src/game/items.ts`。

获取方式按优先级：

1. 自有美术批量绘制：版权最清晰，也最容易保持统一风格。
2. 购买明确允许商业使用、修改和再分发到游戏产品内的素材包；保存订单、授权协议和作者信息。
3. 使用 AI 生成初稿，再由美术统一轮廓、光影和透明边，并保留生成记录与所用模型的商用条款。
4. 不建议直接抓取网络图片、游戏截图、品牌商品图或不明授权 emoji 图做正式资产。

重新生成方式：执行 `npm run assets`。详细维护规则见《素材替换说明.md》；图片无需手工标坐标。

### 5.2 第二优先级：场景与 UI

| 资源 | 数量建议 | 规格/说明 | 来源建议 |
|---|---:|---|---|
| 十分类场景底纹/背景 | 10 | 当前已用 CSS 宣纸渐变实现；后续可换为同色系轻纹理图 | 自有美术或可商用纹理素材 |
| 游戏 Logo | 1 | 透明 PNG/WebP，约 1200×600 | 品牌设计稿 |
| 分享图 | 1~3 | 1:1，至少 512×512，上传 HTTPS CDN | Logo+核心场景合成 |
| 应用图标/头像 | 1 套 | 1024×1024 母版，按平台导出 | 品牌设计稿 |
| 伙伴立绘 | 8 | 透明底，建议 1024×1024；未招募剪影可同图生成 | 自有美术/AI 初稿+人工统一 |
| 道具图标 | 4 | 放大镜、沙漏、鸟瞰眼镜、打捞怀表，512×512 | 自有 UI 美术 |
| 称号/赛季框 | 5+ | SVG 或透明 WebP，注意小尺寸可读性 | 自有 UI 美术 |
| 特效序列/粒子 | 4~8 | 命中、连击、三星、Boss、伙伴招募 | CSS/Canvas 优先，必要时 WebP 序列 |

### 5.3 音频

当前命中、错误、胜利、失败等音效由 Web Audio 合成，可以上线但质感偏 demo。正式版建议准备：点击、命中、错误、连击、倒计时、提示、过关、失败、按钮、Boss 结算共 10~15 个短音效，以及 2~3 组古风氛围循环 BGM。

- 音效优先 OGG + AAC/MP3 兼容，单条尽量 <100 KB。
- BGM 单曲控制在 1~2 MB，循环点无爆音。
- 必须有静音开关，默认遵循移动浏览器“用户首次交互后才能播放”的限制。
- 素材来源必须保留商业授权凭证；避免使用版权不明的短视频热门音乐。

### 5.4 如果改为“整张手绘场景找物”

这会从当前“随机散件”升级为经典手绘寻物，工作量明显更大。每张场景需提供大图和目标热区坐标：`x / 图片宽 × 100`、`y / 图片高 × 100`。建议先做 3 张纵向样板图验证点击精度，再决定是否全面切换。具体数据格式见 `素材替换说明.md`。

## 六、后续开发任务优先级

### P0：正式首发前

- 对 100 关进行真机试玩，校准通过率、三星率和特殊关强度。
- L1 拖动、L2 连击、L3 计分教学已完成；正式发布前复验首次进入、逐关触发、跳过与本地记忆。
- 加关卡选择与四道篇章星门。
- 替换正式 Logo、分享图；123 件寻物素材已经接入。
- 填写真实大神后台参数，完成 App 内/H5/小程序环境验收。
- 运行 360×640、主流 iPhone、主流安卓和桌面兼容测试。

### P1：增强留存

- 眼力值体力、在线/离线恢复与每日补满已完成；道具经济仍待实现。
- 123 件百物图鉴已完成基础点亮；图鉴页面、未点亮保底与稀有度待后续设计。
- 8 名伙伴、伙伴小屋、称号段位。
- Boss 首通与小章奖励。
- 激励视频与分享奖励只在签发、验签、幂等方案确认后接入。

### P2/P3：必须依赖可信服务

- 正式排行榜、赛季、跨设备存档、风控和实物/高价值奖励。
- UGC 关卡签名、公开池、审核、举报、有效完成奖励。
- 自定义游戏内任务的可信服务端完成上报。

如果坚持永远不增加任何服务端，这一阶段只能做“本机个人纪录/本机创作分享码”，不能承诺公平榜单和可信奖励。

## 七、上线验收清单

| 类别 | 验收标准 |
|---|---|
| 构建 | `npm run check` 通过；核心 JS gzip ≤150 KB |
| 首屏 | 360×640 可操作，无横向溢出，无白屏 |
| 基础玩法 | 闯关、无尽、暂停、提示、点错、过关、失败、重试均正常 |
| 存档 | 刷新后关卡、星级、最高分保留；禁用存储时明确接受会话内降级 |
| 关卡 | 正式开放范围内的类型标签与真实规则一致 |
| 大神 | 登录、Ulink、分享菜单、导航栏、任务面板在目标环境分别验收 |
| 统计 | 页面访问、错误、开始、过关、失败等事件能在对应看板找到 |
| 安全 | 无密钥写入前端；无不明外链；奖励不由本地布尔值直接发放 |
| 素材 | 所有正式素材有商业授权记录；分享图是完整 HTTPS 地址 |
| 打包 | ZIP 根目录为 `index.html + assets/`，不包含源码、`.env.local`、测试文件或私钥 |

## 八、本轮交付状态

- 代码规范检查：通过。
- TypeScript 生产构建：通过。
- 本轮已完成生产构建与 HTTP/资源冒烟；正式发布前仍需在 360×640、主流 iPhone 和安卓设备完成视觉与触控复验。
- 当前构建体积：核心 JS gzip 约 127 KB，低于 150 KB 目标；CSS gzip 约 17 KB。
- 大神后台真实参数：待你按《大神后台配置清单》办理并填写。
- 正式物件素材：123 件 WebP 已接入并通过完整性校验；仍需保存来源与商用授权凭证。
